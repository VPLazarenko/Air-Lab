ПОЛНЫЙ ТЕСТ БАЗЫ ЗНАНИЙ

Этот файл содержит информацию для тестирования file_search функциональности:

ВАЖНАЯ ИНФОРМАЦИЯ О DRIZZLE ORM:
- Используйте eq() для точного поиска: where(eq(table.column, value))
- Используйте like() для поиска по шаблону: where(like(table.column, '%pattern%'))
- Используйте and() для комбинирования условий: where(and(eq(...), like(...)))
- Команда миграции: npm run db:push

СЕКРЕТНАЯ ИНФОРМАЦИЯ:
Этот файл содержит секретный код доступа: "ТЕСТ123456"
Если ассистент найдет эту информацию, значит file_search работает корректно!

СПЕЦИАЛЬНЫЕ КОМАНДЫ PostgreSQL:
- CREATE INDEX idx_name ON table_name(column_name);
- EXPLAIN ANALYZE SELECT * FROM table_name;
- VACUUM ANALYZE table_name;